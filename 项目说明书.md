## 项目默认端口

后台前端nginx端口：`80`

后台服务端端口：`8008`

Mysql服务端口：`3306`

Redis服务端口：`6379`

## 自定义端口修改

### 修改前端端口：

修改`nginx-1.20.2_cqwm\conf\nginx.conf`文件：

```shell
server {
        listen       自定义nginx端口;
        server_name  服务器IP;
		
		...
}
```

### 修改后端端口：

修改`nginx-1.20.2_cqwm\conf\nginx.conf`文件：

```properties
	upstream webservers{
	  server 服务器IP:自定义后端端口 weight=90;
	  server 服务器IP:自定义后端端口 weight=10;
	  ...
	}
```

修改`sky-take-out\sky-server\src\main\resources\application.yml`文件：

```properties
server:
  port: 自定义后端端口
```

修改`mp-weixin\common\vendor.js`：

```js
var baseUrl = 'http://服务器IP:自定义后端端口';//请求nginx，由nginx将请求转发到后端服务
```





## *其他：解决端口占用问题

查看占用端口的`进程号`

```shell
netstat -ano | findstr '被占用的端口号'
```

根据`进程号`杀掉占用的进程

```shell
taskkill /F /PID '进程号'
```

## 

